syntax = "proto3";

package EventEngine;

import "GroupedMessage.proto";
import "EventMessage.proto";

//事件引擎服务的接口
service EventEngineService {
    rpc GetEventMessage (GroupedMessage.GroupedMessage) returns (EventMessage.EventMessage);
}